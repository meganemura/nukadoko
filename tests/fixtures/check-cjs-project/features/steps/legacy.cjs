// Exists only to prove `nuka check` names a .cjs file instead of importing
// it. The syntax error below is
// deliberate: if discovery ever tried to import this file, the failure
// would be loud and immediate rather than a step silently going missing.
this is not valid javascript and must never be evaluated by anything;
