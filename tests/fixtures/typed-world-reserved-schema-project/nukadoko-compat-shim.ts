// Fixture-only re-export — see tests/fixtures/compat-project/
// nukadoko-compat-shim.ts's own comment for why this is a relative shim
// rather than the bare "nukadoko/compat" specifier.
export * from "../../../src/compat/index.js";
