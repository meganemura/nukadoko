This directory exists on disk but holds no `.ts`/`.mts`/`.js`/`.mjs` file --
the fixture for `no-step-files-found`. This
file itself is not a step definition; no extension discovery walks matches
`.md`.
