import { defineConfig } from "./nukadoko-shim.js";

// One scenario, two steps, three distinct line numbers (the Scenario
// header, the Given, the Then) — see features/probe.feature and this
// project's own step file for which finding each line is about.
export default defineConfig({});
