Feature: a feature file with no scenarios at all

  # No Scenario/Scenario Outline below on purpose: selectPickles() (src/run/
  # select-pickles.ts) returns zero pickles for this file, exercising m22-
  # compat-run-scope task spec, item 2's "pickle が 1 つも選択されていない場合
  # は実行しない".
