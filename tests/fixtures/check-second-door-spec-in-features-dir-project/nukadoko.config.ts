import { defineConfig } from "./nukadoko-shim.js";

// Exists only to give `nuka check` a project to walk; the one file under
// features/ (cart.spec.ts) is what this fixture tests.
export default defineConfig({});
