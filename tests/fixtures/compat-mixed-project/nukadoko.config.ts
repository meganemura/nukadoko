import { defineConfig } from "./nukadoko-shim.js";

// tests/compat-mixed.test.ts overwrites this file's `baseURL` at run time
// with a real test server's address (same convention as run-session-
// project).
export default defineConfig({});
