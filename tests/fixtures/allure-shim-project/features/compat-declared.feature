Feature: compat step declares allure metadata

  Scenario: a compat step calls allure attachment, label, link, parameter, and a logged step
    Given a compat step declares allure attachment label link parameter and a logged step
