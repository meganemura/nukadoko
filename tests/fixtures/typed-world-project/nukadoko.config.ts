import { defineConfig } from "./nukadoko-shim.js";

// No baseURL: every scenario in this fixture stays off the wire (World
// instrumentation mechanics only, m2c-typed-world task spec).
export default defineConfig({});
