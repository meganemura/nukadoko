import { defineConfig } from "./nukadoko-shim.js";

// A project with only compat steps, no typed ones — tests/compat-discover
// .test.ts and tests/compat-cli.test.ts's fixture for compat's own
// discovery/CLI surface.
export default defineConfig({});
