import { defineConfig } from "./nukadoko-shim.js";

// tests/promotion-comparison.test.ts overwrites this file's `baseURL` at
// run time with a real test server's address (same convention as
// compat-mixed-project/run-session-project).
export default defineConfig({});
