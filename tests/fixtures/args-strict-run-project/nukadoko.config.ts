import { defineConfig } from "./nukadoko-shim.js";

// A pure-step project, on purpose: exercises `nuka run`'s own args
// validation without needing a browser or an HTTP server.
export default defineConfig({});
