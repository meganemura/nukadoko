// Exists only to prove discovery names a .cjs file instead of importing it
// (p10-step-discovery task spec, scope 3) -- the syntax error below is
// deliberate: if discoverSteps ever tried to import this file, the failure
// would be loud and immediate rather than a step silently going missing.
this is not valid javascript and must never be evaluated by anything;
